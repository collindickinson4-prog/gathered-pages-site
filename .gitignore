# Lavish review sessions — local preview copies and screenshots
.lavish/

# Original shared-drive dump and the zip it came from — source material, not the site
source/
*.zip

# Vercel local link state
.vercel/

node_modules/
.DS_Store
Thumbs.db

.vercel

# Secrets - Kit API key lives here locally
.env
.env.local
